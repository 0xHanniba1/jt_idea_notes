# Interface icons

The `heroicons-*.svg` filenames are retained for the existing import and sprite pipeline. Their artwork is adapted from Lucide 1.33.0, matching jt_case_platform at d71a4d7, rendered with a 1.7 stroke width. Lucide is ISC licensed; see LICENSE-lucide. No runtime dependency was added. Brand logos and user images are unchanged.

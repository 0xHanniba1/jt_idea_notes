# JT Idea Notes 项目约定

本文件于 2026-09-16 从 `jt_case_platform/AGENTS.md` 迁入，保留通用协作流程，并按 Fider 的技术栈和本项目用途调整项目边界。

- 本项目目录为 `/Users/kerwin/JTRAG/jt_idea_notes`，从 Fider Fork 后独立维护；默认远程仓库为 `0xHanniba1/jt_idea_notes`，官方上游为 `getfider/fider`。
- 产品用于方便记录想法、需求、Bug 和优化建议，先供本人和领导使用，后续供同事使用，以电脑端为主。记录优先，状态用于说明进展；不擅自增加审批、强制处理结论或复杂流程。
- 案件平台 `jt_case_platform` 提供设计规范参考，其代码、业务、数据和运行配置不属于本项目修改范围。
- 当前技术栈为 Go、React、TypeScript、SCSS 和 PostgreSQL。保留 Fider 的现有结构、组件与构建方式，不套用案件平台的 Next.js 规则。
- 原版试用环境位于相邻的 `fider-local` 目录。开发与测试使用独立的数据、端口、登录 Cookie 和镜像标识；未经授权不覆盖试用环境，不接入其他项目的真实数据。
- 上游 README、CLAUDE.md 和历史记录包含 Fider 原版说明；不能据此声称本项目的定制功能或运行环境已验证。当前状态以实际代码、配置和检查结果为准。

## 前端设计参考

新增或调整前端页面、组件与交互前，先阅读
[`docs/frontend-design-references.md`](./docs/frontend-design-references.md)。UI 组件外观与交互以 `jt_case_platform` 的实际实现及最新设计规则为基准，页面采用已确认的左侧全局导航、顶部路径栏与白色工作区，内容保持紧凑列表与右侧详情抽屉。Fider 保留业务逻辑、技术栈及构建方式。外壳方案见 [`docs/jt-workspace-shell-plan.md`](./docs/jt-workspace-shell-plan.md)，控件映射见 [`docs/jt-ui-refresh-plan.md`](./docs/jt-ui-refresh-plan.md)；外部网站仅作补充参考。

## 代码与验证

- 按改动范围阅读 [`GUIDELINES.md`](./GUIDELINES.md)、[`CLAUDE.md`](./CLAUDE.md) 和相关代码；本项目边界与设计规则以本文件和前端设计参考为准。
- 前端页面位于 `public/pages/`，共享组件位于 `public/components/`，全局样式位于 `public/assets/styles/`，翻译位于 `locale/`；优先复用已有组件、BEM 命名和工具类，不直接使用项目未引入的 Tailwind 类名。
- 开发、构建与检查命令以 [`Makefile`](./Makefile) 为准；运行时与依赖版本以 [`package.json`](./package.json)、[`.nvmrc`](./.nvmrc) 和 [`go.mod`](./go.mod) 为准，不照搬旧文档中的版本号。
- 代码变更执行相关 lint、测试和必要构建；界面与交互变更须实际检查受影响页面。涉及数据库的验证先确认 `.test.env` 指向隔离测试库。
- 仅文档变更检查内容、相对链接与 `git diff --check`，无需为此安装依赖、启动服务或运行数据库迁移。

## Issue 与 PR 工作流

- 非平凡功能和修复默认采用 Issue → 独立分支 → PR；纯讨论、只读评审及小型文案或样式调整可以简化。
- 开始任务时优先读取已有 Issue 和关联 PR；没有 Issue 时，先整理目标、范围和验收条件，按用户授权创建，避免重复建单。
- 继续同一未完成任务时，复用已有分支和 PR；新任务使用独立的 `codex/` 分支，保留无关未提交改动。
- 任务范围、结论或状态发生实质变化时，在已有授权范围内更新 Issue，记录当前结论、已完成项和下一步，并链接相关文档与 PR。
- 完成必要检查和实际行为验证后，整理 PR：关联 Issue，说明改动、验证结果和剩余问题。
- 创建或更新 GitHub 记录、提交和推送按用户授权执行；已有授权持续有效，不因阶段切换重复确认。
- 合并须经用户明确批准；完成验收并合并后，关闭对应 Issue。交付时区分已实现、已验证、已合并和已部署。

### 一次授权完成收尾

- 用户明确说“验收通过，交付到 main”“交付到 main”或同等含义时，即授权对本次已验收范围完成全部收尾，无需逐步重复确认。
- 依次完成：必要检查 → 更新 Changelog → 提交并推送本次分支、创建或更新 PR → 合并并推送 `main` → 同步本地 `main`、核对远端提交一致 → 更新并关闭已完成的对应 Issue → 清理本次已合并的本地、远端分支及临时工作区。按上文允许简化的小型改动执行时，可省略 Issue 或 PR。
- 已通过且对应代码未变化的检查沿用现有结果；遇到新改动、失败或合并冲突时补做相应验证。核对远端成功后才清理分支；清理前确认其改动已合并、没有未提交内容且未被其他任务使用。
- 无关未提交工作和本地测试数据继续保留，仅提交本次相关文件。最终说明提交与合并结果、远端同步、分支清理及保留的未提交工作。
- 本条授权覆盖代码交付；部署、真实数据迁移及真实账号或密钥变更，仍按其已有授权分别处理。

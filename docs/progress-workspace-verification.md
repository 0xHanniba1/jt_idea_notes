# 工作区细节与进度页改造

关联Issue #16，分支codex/progress-workspace；2026-09-17。

## 改动

- 工作区品牌显示金唐／需求工作台，保留自定义Logo与数据库站点设置。折叠按钮根据展开状态切换箭头方向，图标面板线不翻转。
- 通知浮层320px宽，内部滚动上限360px，无新通知14px；窄屏保持12px边距。
- 进度页采用计划中／进行中／完成胶囊分段按钮，一次展示一个状态的全宽列表。参考jt_crm管理看板的tabs/report-period-tabs，适配既有主题变量。
- 复用首页PostsContainer与分页API：默认25条，可选10/25/50/100；搜索和状态切换回到第一页，刷新保留状态、搜索、页码。服务端只查询选中状态；状态白名单和既有权限边界保留。
- 保留详情抽屉、异步过期响应防护、失败重试和后台刷新。空状态仍保留搜索、分段按钮和分页；无搜索时显示“当前状态下暂无记录”。

## 验证

- 29套260项前端测试通过，包含状态切换、分页参数、URL路径、隐藏条件清理、空状态和过期响应测试；最终空状态文案调整后重跑相关21项通过。
- 类型检查、全量lint、SSR、生产前端和Go构建通过。保留既有2处lint警告与构建资源大小/C++警告。
- Roadmap handler测试覆盖三个状态、非法状态回退、query/page/limit传递、一次查询、非Pro限制及失败处理；使用隔离测试环境。
- 4184 Chrome实测计划中13条、每页10条第二页11–13，刷新仍保持第二页；无结果搜索重置第一页并保留入口。详情修改合成记录状态后计划中降至12条，关闭返回原进度URL，切到进行中显示对应记录；完成状态及刷新也正确。
- 折叠按钮箭头向右，展开时向左；通知空态字号和320px浮层实际视觉符合预期。390px视口检查通知不越界，分段按钮、搜索与分页自动换行；随后恢复原视口。
- 仅使用隔离4184数据。新建的11条分页合成记录已通过正常软删除清理，原数据保留。

## 分页主题补充验收

- 首页与进度页的每页条数选择器改用现有主题Dropdown，展开菜单的背景、文字、边框和选中态随浅色／深色主题切换。保留键盘操作、选中项提示和加载禁用。
- 相关2套25项测试、类型检查、定向lint、SSR和生产前端构建通过。4184 Chrome实测浅色与深色展开菜单，10／25条切换正常；检查后恢复浅色与25条。

## 当前状态

已完成4184预览验收；用户随后授权交付main并更新4180。沿用对应代码的已通过检查，发布前完成只读评审和产物校验，4180停写备份后仅替换应用镜像并核对业务数据。无需数据库迁移；最终PR、提交与4180更新结果同步至Issue #16。云端不在本次更新范围。

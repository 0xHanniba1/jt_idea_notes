# 全局导航与白色工作区验收

2026-09-17，关联 [Issue #11](https://github.com/0xHanniba1/jt_idea_notes/issues/11)。分支 `codex/jt-workspace-shell`，基于 main `636c3403`。

## 已实现

- 全局180px左导航，64px折叠；白色主工作区、8px外边距、12px圆角、50px路径栏；账号入口固定在导航底部。
- 首页、路线图、详情、设置、账号管理、个人设置、通知及错误页接入统一外壳，列表＋详情抽屉模式保留。
- 原权限、API和数据不变；中文文案接入原翻译系统，其他语言继续回退英语。
- 桌面折叠偏好可保存；680px及以下为导航抽屉，复用Modal焦点、背景inert、Escape及焦点恢复。
- 审核提醒改为带数量的紧凑入口，完整说明仍供辅助技术读取；开发环境标记移到右下角，避免覆盖品牌导航。

## 自动检查

| 检查 | 结果 |
| --- | --- |
| `tmp/password-auth/test-env make test` | 通过。Go short/race检查通过；前端25套、230项通过 |
| `npx tsc --noEmit --skipLibCheck` | 通过，沿用既有依赖声明排除方式 |
| `tmp/password-auth/test-env make lint` | 通过，保留fider.ts中2条既有非空断言警告 |
| `make build-ssr build-ui` | 通过，Webpack保留3条既有构建警告 |
| `git diff --check` | 通过 |

新Header的5项行为检查覆盖角色导航、折叠保存且不丢输入、详情历史变化不改变来源导航、窄屏焦点圈定/恢复/断点切换、跳过导航。SSR既有检查同步验证单一main与首页内容；首次检查暴露V8 shim不提供pathname的问题，增加空值回退后通过，未放宽原功能或安全断言。

## 实际浏览器

使用 `http://jt-accounts.localhost:4184` 的既有隔离开发环境与合成账号。浏览器检查中添加一条合成想法（#42）及一条验收评论；未改真实站点账号或数据。

- 桌面1680px和1440px：实际侧栏180px、折叠64px；主面板从x=180开始，上右下各8px，顶栏50px；页面纵向内容独立滚动。
- 1024px：详情、个人设置、站点设置均无整页横向溢出；账号菜单和通知菜单可操作。
- 390px：路线图与详情无整页横向溢出；打开左导航后背景不可操作，Escape先关闭账号菜单、再关闭导航并将焦点还给导航按钮；切换页面后导航关闭。
- 首页搜索“界面验收”、选择“开启”筛选，打开详情并关闭后query/status保留，焦点回到记录。
- 长列表打开#26详情，关闭前后实际工作区scrollTop均为612.5，焦点恢复到#26；自动点击工具在点击前会将记录滚至可见位置，未把点击前的工具滚动量当作应用回归。
- 路线图打开记录，保持路线图选中；Escape关闭详情后回到原记录。
- 窄屏实际新建记录、保存并进入详情、发表评论成功；此前的列表、评论数据仍在。
- 深色主题下导航、白色面板对应的深色表面、路线图状态与文字可读；检查结束恢复浅色与桌面尺寸。

只读子Agent审查发现的SSR路径、紧凑审核提醒、通知按钮选择器、游客折叠入口和最小触控宽度均已处理。生产构建清理未识别动态根类名的问题已通过静态类名修复并在实际浏览器核验。

## 名称调整

用户随后确认将“所有反馈”改为“首页”。已统一侧栏、首页路径栏和详情返回路径，构建及浏览器检查通过，4184已更新。用户随后确认“路线图”改为“进度”，已统一侧栏、路径栏、页面和浏览器标题、返回入口及空状态说明。前后端构建通过，4184浏览器实际标题为“进度 · 想法簿”，侧栏、路径栏和页面标题已核对。

## 交付状态

本轮为实现与本地预览。Issue验收清单已同步，等待用户指令交付main；未提交、推送、合并或部署到4180/云服务器。4180继续使用main既有镜像，案件平台仅只读参考。保留4184数据与忽略目录下日志。

# 内部账号与主题控件验收

2026-09-17；Issue #18；分支 codex/internal-workspace-controls。

## 实现

- 移除成员信任按钮、内容审核开关、审核队列及审批操作，删除相应后端路由和服务。新记录/评论直接发布；旧moderation和pending筛选参数忽略。保留租户隔离、账号权限、私有标签和软删除规则。
- 追加迁移202609171800，关闭历史审核开关、批准历史待审内容并设置直接发布默认值；保留历史数据库列，不恢复软删除记录或封禁账号。
- 全站原生select扫描为0；共享Select复用主题Dropdown，覆盖创建账号/调整角色、账号角色/状态筛选、站点语言、记录状态和事件回调类型。菜单使用主题颜色和滚动条，支持标签、错误关联、受控值、禁用、键盘及字母定位。
- 登录标题显示金唐需求工作台，登录/首次改密主题按钮在卡片内右上；首次改密退出与保存同一行。退出不提交密码表单，保存与退出互斥。

## 检查

- 前端31套269项测试、TypeScript、全量前端lint通过；SSR与前后端构建通过。保留既有构建资源大小及C++警告。
- Go handlers/API/actions/models/dbEntities及postgres/cmd/middlewares检查通过；隔离测试库jt_idea_auth_test。真实数据库测试覆盖普通成员直接发布、他人可见、迁移幂等、删除/封禁保持。
- 4184开发库jt_idea_accounts_dev迁移前完整备份于tmp/internal-controls/backup/database.dump，归档清单验证通过；迁移由97条增至98条。未操作4180或云端数据库。
- Chrome实际验证浅深色创建角色/角色筛选菜单，筛选结果正确；长语言菜单滚动与弹窗角色菜单可见；账号无信任按钮、访问权限无审核开关。
- 实际登录合成临时账号验证金唐需求工作台标题、卡片内主题按钮、首次改密同行按钮、390px深色布局，以及未填写密码直接退出成功。未在浏览器修改密码；合成账号qa_card_1789627753已停用，恢复原开发管理员会话、浅色和原视口。
- 审核管理页、审核计数、信任/取消信任及审批API实际请求均返回404。
- 独立只读集成评审无阻塞项。

## 状态

已完成4184预览验收。用户随后授权交付main并更新4180，沿用未变更代码的已通过检查和只读评审；发布前备份数据库与配置，迁移后核对业务数据、权限与实际界面。最终PR、main提交和4180更新结果记录在Issue #18。云端不在本次范围。

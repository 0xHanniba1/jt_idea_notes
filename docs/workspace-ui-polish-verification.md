# 工作区细节优化验收

日期：2026-09-17；Issue #14；分支 `codex/workspace-ui-polish`。

## 修改

- 展开侧栏的账号菜单宽度跟随底部账号按钮；折叠侧栏时保留160px可读宽度。
- 移除主导航中的站点设置；仅管理员账号菜单显示该入口，移除菜单内重复昵称与“管理”分组。底部账号身份保留，后端既有权限不变。
- 铃铛外层使用居中inline-flex，消除文字行高造成的视觉偏移，未读角标保留。
- 新建想法不显示标题/描述占位说明；发布区始终可见，不满足现有标题校验或只读/提交中时禁用。原先标题不足10个JS字符时整块发布区不渲染。

## 验证

- 前端27套、240项测试通过；其中Header覆盖管理员/协作者/普通成员菜单可见性，ShareFeedback覆盖常驻按钮、有效性、只读、重复提交防护、失败恢复、模板和附件保留。
- TypeScript、全量lint通过（2处既有非空断言警告）；SSR及生产前端构建通过（3项既有资源大小警告）；git diff --check通过。
- 隔离4184 Chrome实测：账号按钮与菜单left均12px、width均158px；铃铛与按钮中心x/y偏差均0px；通知可打开；菜单只含我的设置、站点设置、退出。
- 折叠导航后菜单160px宽，完整可读；Escape可关闭并恢复焦点。
- 新建空表单无占位说明、提交按钮可见且禁用；9字符标题仍禁用，填写有效标题和描述后启用。实际发布记录#46并刷新，标题及正文持久化正确。
- 测试仅使用独立4184合成数据，未更新4180或云端，尚未提交/合并main。

## 后续调整（2026-09-17）

按最新截图移除账号菜单两条分隔线；标题输入移到内容前，打开弹窗直接聚焦标题，标题字段文案简化为“标题”，主按钮改为“发布”（复用现有中英文翻译）。相关13项测试、TypeScript、ESLint、SSR与前端构建通过；4184浏览器确认菜单无分隔线、标题在前且自动聚焦，按钮文案正确。标签功能保持原样：启用发帖选标签且存在可见标签时显示，为可选分类，不是本轮新增功能。尚未交付main或更新4180。

## 新建弹窗精简（2026-09-17）

按用户确认移出新建弹窗标签选择，并删除发布区上方分隔线；详情页标签功能保留。新建提交不再从旧草稿或URL筛选条件隐式携带标签。7项相关测试、TypeScript、ESLint、SSR/前端构建通过；4184浏览器实测无标签区且分隔线border-top为0px，标题优先和常驻发布按钮保留。未更新main/4180。

## 标题规则统一（2026-09-17）

用户确认将新建和编辑标题统一为1–100个Unicode码点，不得全为空白。首尾空白去除，连续Unicode空白合并为一个空格；前端Array.from与后端RuneCount计数一致，普通汉字、字母、单码点表情均算1个。移除HTML maxlength的UTF-16限制，超限时禁用发布/保存，API仍独立校验。规范化值参与重复检查和存储。

实际HTTP验证发现历史posts.slug仅varchar(100)，中文转拼音后超长会导致500。因此增加向前兼容迁移202609171200_expand_post_slug.sql，将slug扩为TEXT，保持既有网址生成方式及唯一约束。4184迁移前备份于tmp/ui-polish/title-backup/database.dump并验证归档清单；4180未迁移。

验证：Go全app短测试通过；新迁移后真实PostgreSQL长slug创建/更新/读取及唯一性3项测试通过；受影响前端21项测试、类型检查、全量lint、SSR及前后端构建通过（既有警告保留）。4184浏览器验证“登录报错”、100中文、100单码点emoji可启用发布；101中文和全空白禁用。实际API新建及编辑验证1/100/101、全空白、中文/emoji、规范化持久化和重复标题拒绝；合成记录48–50，48最终标题为“登录报错”。首次失败前创建的合成记录47保留。未交付main/4180/云端。


## 关闭新建弹窗后清空（2026-09-17）

- [x] 每次打开“记一个想法”创建独立表单；关闭后标题、内容、附件及错误状态不留存。
- [x] 移除新建草稿的本地缓存读写，并清理旧版本缓存；新建时不自动填入描述模板，始终从空白开始。
- [x] 提交失败时保留当前打开弹窗的内容与附件，允许重试。
- [x] 8项相关测试、TypeScript、ESLint、SSR及生产前端构建通过；4184浏览器实测关闭按钮和Escape关闭后再次打开，标题和内容均为空、发布禁用。

本次已更新4184预览，尚未提交/合并main，未更新4180或云端。


## 列表分页（2026-09-17）

- [x] 移除“查看更多帖子”，列表底部显示总条数、当前范围、每页条数、页码及上一页/下一页；默认25条，可选10/25/50/100。
- [x] 服务端按相同搜索、筛选和权限条件统计总数并分页；保留旧API数组响应，新分页请求显式传page。相同排序值用ID稳定排序，越界页码自动回到有效页。
- [x] 搜索、筛选、排序、每页条数变更回到第一页；URL保留分页条件，刷新仍保留页码；空结果和第一页/最后一页正确禁用按钮。详情抽屉后台刷新不改写详情URL。
- [x] 前端27套246项测试、TypeScript、相关ESLint、Go handler及真实隔离PostgreSQL测试、后端lint均通过；SSR和前后端构建通过，保留既有构建警告。
- [x] 4184浏览器实测37条记录：25条模式第二页12条，10条模式页码正确；刷新第二页仍显示11–20，搜索无结果显示0条并重置第一页；底部分页栏视觉及键盘焦点已检查。

已更新4184预览；本次不需要数据库迁移。未提交/合并main，未更新4180或云端。


## 内部账号仅停用，不允许自行删除（2026-09-17）

- [x] 从“我的设置”移除删除账户说明、按钮和确认弹窗；保留昵称、头像、站内通知与修改密码。
- [x] 移除DELETE /_api/user路由、前端调用和后端自删命令/存储逻辑，不能绕过界面调用旧接口删除或匿名化账号。
- [x] 保留管理员停用/恢复流程。隔离数据库测试确认停用保留姓名、记录作者归属和历史记录；既有密码账号管理与权限测试通过。
- [x] 个人设置3项测试、TypeScript、全量lint、相关Go测试和SSR/前后端构建通过（既有警告保留）。4184真实请求旧删除接口返回404，原会话仍可使用；浏览器“我的设置”确认无删除入口、昵称和改密功能仍在。

已更新4184；无需数据库迁移或修改现有账号。未提交/合并main，未更新4180或云端。


## 个人设置按 CRM 分模块与头像移植（2026-09-17）

- [x] 参考jt_crm的profile-center/profile-avatar与accounts/avatar/auth样式，将个人设置调整为个人资料、登录密码、站内通知卡片；桌面双列、900px以下单列，沿用现有主题变量。用户名和角色只读，昵称可编辑。
- [x] 删除头像类型下拉，使用头像预览、上传/更换/移除；默认头像与侧栏按昵称首个Unicode字符显示，图片失败同样回退。JPG/PNG/WebP原图最大5MB，拒绝动画，裁剪支持拖动/方向键、1–3倍缩放与重新居中，输出256×256 PNG。
- [x] 适配Fider既有CSP，预览使用FileReader data URL；取消/卸载释放资源。服务端对头像独立解码与静态PNG重编码，验证格式、正方形、尺寸、512KB上限，不采信客户端blob引用。
- [x] 资料、头像、通知通过独立API只写本模块字段，避免相互覆盖；密码沿用现有8–12位规则和会话失效行为。失败保留输入，提交防重复。
- [x] 前端相关测试（含卡片与独立保存、默认头像、裁剪及资源清理）通过；全套前端测试中发现的FileReader mock问题已修复并重跑受影响8项通过，其他28套通过。TypeScript、相关ESLint、Go actions/handlers回归、隔离数据库字段隔离测试、Go lint与SSR/前后端构建通过，保留既有构建警告。
- [x] 4184浏览器完成真实上传→缩放/键盘调整→保存→刷新读取→移除首字回退；上传/保存通知均不覆盖未保存昵称；通知切换保存刷新有效。恢复合成验收账号原昵称、默认头像及通知偏好。390px视口卡片单列，无横向溢出。

已更新4184，Issue仍待交付。未提交/合并main，未更新4180或云端；无需数据库迁移。


## 交付检查（2026-09-17）

用户已授权交付main并更新4180。最终聚合检查：29套257项前端测试、全app Go短测试（隔离测试库）、TypeScript和全量lint通过；lint保留2处既有警告。独立只读评审未发现阻塞项。此前各节的“尚未交付”记录的是当时状态；最终PR、main提交及4180部署/备份结果同步至Issue #14。

本批包含一次迁移：posts.slug扩为TEXT；其他分页和个人设置调整不需要新增迁移。4180更新前备份完整数据库及配置，迁移后检查业务数据摘要不变，保留旧镜像以便仅回退应用。云服务器不在本次更新范围。
